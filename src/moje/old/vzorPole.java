package moje.old;

public class vzorPole {
    /*
    public static void main(String[] args) {
        DataImport di = new DataImport("");

        ArrayList<Třída>  pole = new ArrayList<>();
        while (di.hasNext()) {
            Třída vzor = parseLine(di.readLine());
            pole.add(vzor);
        }



        di.finishImport();
    }

    static Třída parseLine(String Line) {
        String[] dataPieces = Line.split(";");

        Třída vzor = new Třída(
                dataPieces[0],
                dataPieces[1],
                dataPieces[2],
                dataPieces[3]
        );

        return vzor;
    }
    */

    //Třída
}
